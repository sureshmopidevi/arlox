# [[.Name]] Backend

Spring Boot, JPA, and Maven backend for [[.DisplayName]].

Requires Java 21 and Maven.

```bash
cp .env.example .env
mvn spring-boot:run
```

Export values from `.env` when overriding defaults. Run `mvn test` and
`mvn package` before committing.
