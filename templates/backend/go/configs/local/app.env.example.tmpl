SERVER_PORT=8080
SERVER_ENV=development
APP_NAME=[[.DisplayName]]

DB_HOST=localhost
DB_PORT=5432
# Leave DB_USER empty to use your OS username (recommended for local Homebrew Postgres).
DB_USER=
# Leave DB_PASSWORD empty for local trust/peer auth.
DB_PASSWORD=
DB_NAME=[[.Name]]
DB_SSLMODE=disable

JWT_SECRET=change-me-in-production
JWT_EXPIRY_HOURS=24
