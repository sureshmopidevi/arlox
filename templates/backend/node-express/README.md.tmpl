# [[.Name]] Backend

Express, TypeScript, and Prisma backend for [[.DisplayName]].

```bash
npm install
cp .env.example .env
npm run db:generate
npm run db:migrate
npm run dev
```

Run `npm test` and `npm run build` before committing.
