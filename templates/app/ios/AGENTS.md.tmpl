# [[.DisplayName]] — iOS Agent Guide

This package uses SwiftUI, XCTest, MVVM, and repository boundaries. Keep the
feature library independent from app composition where practical.

Run `swift test` before finishing. Use Xcode for iOS simulator, signing,
capabilities, assets, previews, and archive validation. Record feature
decisions in `.cursor/skills/add-feature-ios/learned/README.md`.
