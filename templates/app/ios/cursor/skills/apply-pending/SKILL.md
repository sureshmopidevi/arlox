---
name: apply-pending
description: Review and apply deferred iOS learnings safely.
---

# Apply Pending

1. Read `add-feature-ios/learned/README.md`.
2. Process `[PENDING]` entries oldest first.
3. Confirm each still applies; ask if app-target or Xcode requirements are
   ambiguous.
4. Implement one entry at a time and run `swift test`.
5. Remove `[PENDING]` and record the resolution, including any Xcode-only
   validation still required.
