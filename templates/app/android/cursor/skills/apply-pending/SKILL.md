---
name: apply-pending
description: Review and apply deferred Android learnings safely.
---

# Apply Pending

1. Read `add-feature-android/learned/README.md`.
2. Process `[PENDING]` entries oldest first.
3. Confirm each still applies; ask if the feature scope is ambiguous.
4. Implement one entry at a time and run `gradle test lint assembleDebug`.
5. Remove `[PENDING]` and record the resolution and any required device check.
