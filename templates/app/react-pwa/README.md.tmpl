# [[.DisplayName]]

Offline-ready React PWA generated for `[[.Org]]`.

```bash
npm install
npm run dev
npm test
npm run build
```

The service worker is generated by `vite-plugin-pwa` during production builds.
Use the browser's Application panel or an offline Lighthouse run to verify
installation and cached navigation.
