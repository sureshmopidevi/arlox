---
name: apply-pending
description: Review and apply deferred React PWA learnings safely.
---

# Apply Pending

1. Read `add-feature-pwa/learned/README.md`.
2. Process `[PENDING]` entries oldest first.
3. Confirm each still applies; ask if its behavior or cache policy is unclear.
4. Implement one entry at a time and run `npm test` plus `npm run build`.
5. Remove `[PENDING]` and record the resolution, or explain why it was
   superseded.
