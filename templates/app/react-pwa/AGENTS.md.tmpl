# [[.DisplayName]] — PWA Agent Guide

Vite, React, TypeScript, `vite-plugin-pwa`, Vitest, and React Testing Library.
The backend base URL is `[[.APIURL]]`.

Before finishing, run `npm test` and `npm run build`. Keep pages accessible,
preserve offline navigation, and follow `.cursor/rules/architecture.mdc`.
Record feature decisions in
`.cursor/skills/add-feature-pwa/learned/README.md`.
