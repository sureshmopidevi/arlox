# [[.DisplayName]] — Agent Guide

**Design system:** [[.WebDesignSystemLabel]] — see `.arlox/design-system.json`.

Use **@mui/material** for all UI. Follow `.cursor/skills/add-feature-web/SKILL.md`.

```bash
npm run build   # must pass
```
