# [[.DisplayName]] — Agent Guide

**Design system:** [[.WebDesignSystemLabel]] — see `.arlox/design-system.json` and `.cursor/rules/design-system.mdc`.

Backend API: `[[.APIURL]]`.

Follow `.cursor/skills/add-feature-web/SKILL.md`. Use **antd** components for all UI.

```bash
npm run build   # must pass
```
